# Define the interactive CLI task contract

Type: grilling
Status: open

## Question

For the two MVP workflows, what exactly can a developer type while a task is active, what progress is visible, and how does the agent distinguish an informational question from an explicit stop request? Define observable behavior without requiring arbitrary mid-task scope changes.

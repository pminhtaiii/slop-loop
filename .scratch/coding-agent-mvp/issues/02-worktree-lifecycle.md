# Define the task worktree lifecycle

Type: grilling
Status: open

## Question

How is a task Git worktree created, named, retained, and cleaned up, and what happens to its diff when a task succeeds, fails, or is stopped? Preserve the developer's current worktree and branch in every case.
